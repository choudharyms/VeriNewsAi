services:
  - type: web
    name: verinewsai-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn main:app -k uvicorn.workers.UvicornWorker
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: PORT
        value: 10000
